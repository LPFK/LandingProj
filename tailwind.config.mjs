/**
 * Tailwind v4 uses a CSS-first config: the design tokens (palette, fonts,
 * radius, shadow) live in `src/styles/globals.css` under the `@theme` block.
 * This file is kept for tooling that still expects a config entry point and to
 * document the content sources. Edit tokens in globals.css, not here.
 */
export default {
  content: ["./src/**/*.{astro,html,js,jsx,ts,tsx,md,mdx}"],
};
